package com.example.backend.domain.ingredient;

import org.springframework.data.jpa.repository.JpaRepository;

public interface ImageRecognitionLogRepository extends JpaRepository<ImageRecognitionLog, Long> {
}
